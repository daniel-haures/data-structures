#ifndef EDIT_DISTANCE_H_fdsakjdssqewpijifegtrvbp
#define ORDERED_ARRAY_H_fdsakjdssqewpijifegtrvbp

//Take in input two strings that can't be NULL and return the edit distance between them with a recursive function
int edit_distance_init(char* s1, char* s2);

//Take in input two strings that can't be NULL and return the edit distance between them with a dynamic recursive function
int dyn_edit_distance_init(char* s1, char* s2);

#endif